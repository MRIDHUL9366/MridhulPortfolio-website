var SE=document.getElementByClassName("SE");
var skills=document.getElementByClassName("skill-table");

function opentap(tabname){
      for(S of SE){
      S.classList.remove("ME");
       }
       for(skill of Skills){
      Skill.classList.remove("active-skill");
       }
}